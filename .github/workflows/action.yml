name: Test
on: [push]
jobs:
  Explore-GitHub-Actions:
    runs-on: ubuntu-latest
    steps:
      - name: Try to install something
        run: |
          docker run -d -p 25565:25565 --name mc itzg/minecraft-server